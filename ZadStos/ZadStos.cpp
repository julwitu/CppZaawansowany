#include <iostream>
#include <stack>

int main()
{
	std::string poem = "Ala ma kota, a kot ma pchly";
	std::cout << "ODWROCONE: ";
	std::stack <char> reverser ;

	auto it = list1.begin();
	std::advance(it, 3);
	second it = list2.begin();
	std::advance(second, 3)

		list2.splice(second, it);

}

